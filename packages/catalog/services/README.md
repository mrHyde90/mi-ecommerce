# catalog-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build catalog-services` to build the library.

## Running unit tests

Run `nx test catalog-services` to execute the unit tests via [Vitest](https://vitest.dev/).
